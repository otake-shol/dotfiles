# ========================================
# dotfiles エイリアス設定
# ========================================
# カテゴリ別に分割されたエイリアスファイルを読み込み
#
# 構成:
#   ~/.zsh/aliases/core.zsh - 基本操作、システム、ネットワーク
#   ~/.zsh/aliases/git.zsh  - Git、GitHub CLI
#   ~/.zsh/aliases/node.zsh - npm、yarn、pnpm、bun
#   ~/.zsh/aliases/dev.zsh  - エディタ、Python、便利関数

ALIASES_DIR="${HOME}/.zsh/aliases"

# 分割ファイルの読み込み
for alias_file in "$ALIASES_DIR"/*.zsh; do
    if [[ -f "$alias_file" ]]; then
        source "$alias_file"
    fi
done

unset ALIASES_DIR
