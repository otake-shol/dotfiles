.DS_Store
*.swp
*~

# Homebrew Bundle lock files
Brewfile.lock.json

# Local settings (contains personal info / secrets)
# Note: *.local.template files are tracked (templates for users)
.gitconfig.local
.zshrc.local
settings.local.json

# Claude Code project-local settings (auto-generated at repo root)
# Note: stow/claude/.claude/ is user config (tracked), this is project-specific (ignored)
/.claude/

# Serena tool internal files
.serena/

# Firebase debug logs
firebase-debug.log

# Playwright MCP screenshots/artifacts
.playwright-mcp/

# Personal resources (managed in cloud storage)
docs/resources/
